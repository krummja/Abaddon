import subprocess

import spiceypy as spy
from pathlib import Path
import devtools as dev


if __name__ == "__main__":
    pass

    # spy.furnsh(str(file_path) + "/de431t.bsp")

    # spy.furnsh(str(file_path) + "/latest_leapseconds.tls")

    # count = spy.ktotal("all")

    # if count == 0:
    #     print("No kernel files loaded at this time.")
    # else:
    #     print("The loaded kernel files are:\n")

    # for which in range(count):
    #     file, file_type, source, handle = spy.kdata(which, "all")
    #     print([file, file_type, source, handle])

    # t_id = spy.bodn2c("Earth")
    # print(t_id)

    # UTC = "2026 JUN 5 00:00:00"
    # et = spy.str2et(UTC)

    # print(et)

    # result = spy.spkezr("SSB", et, "J2000", "NONE", "Earth")
    # print(result)
